-- Existing invalid values must be corrected before applying; strict SQL mode prevents coercion.
SET @previous_sql_mode = @@SESSION.sql_mode;
SET SESSION sql_mode = 'STRICT_ALL_TABLES,NO_ENGINE_SUBSTITUTION';
ALTER TABLE `users` MODIFY `status` ENUM('ACTIVE','INACTIVE','SUSPENDED') NOT NULL DEFAULT 'ACTIVE';
ALTER TABLE `mushaf_categories` MODIFY `status` ENUM('ACTIVE','INACTIVE') NOT NULL DEFAULT 'ACTIVE';
ALTER TABLE `service_types` MODIFY `status` ENUM('ACTIVE','INACTIVE') NOT NULL DEFAULT 'ACTIVE';
ALTER TABLE `service_addons` MODIFY `status` ENUM('ACTIVE','INACTIVE') NOT NULL DEFAULT 'ACTIVE';
ALTER TABLE `distribution_teams` MODIFY `status` ENUM('ACTIVE','INACTIVE') NOT NULL DEFAULT 'ACTIVE';
ALTER TABLE `team_members` MODIFY `status` ENUM('ACTIVE','INACTIVE') NOT NULL DEFAULT 'ACTIVE';
ALTER TABLE `publisher_documents` MODIFY `status` ENUM('PENDING','APPROVED','REJECTED') NOT NULL DEFAULT 'PENDING';
ALTER TABLE `registrations` MODIFY `status` ENUM('DRAFT','READY_FOR_VERIFICATION','IN_VERIFICATION','REVISION_REQUIRED','WAITING_VERIFICATION_APPROVAL','AWAITING_PAYMENT','PAYMENT_VERIFICATION','WAITING_DISTRIBUTION','TASHIH_IN_PROGRESS','READY_FOR_STT','STT_ISSUED','DOCUMENTATION_IN_PROGRESS','COMPLETED','CANCELLED') NOT NULL DEFAULT 'DRAFT';
ALTER TABLE `verification_assignments` MODIFY `status` ENUM('ASSIGNED','IN_PROGRESS','COMPLETED') NOT NULL DEFAULT 'ASSIGNED';
ALTER TABLE `payment_records` MODIFY `status` ENUM('UNPAID','PAID','VERIFIED','EXPIRED','CANCELLED') NOT NULL DEFAULT 'UNPAID';
ALTER TABLE `assignments` MODIFY `status` ENUM('ASSIGNED','IN_PROGRESS','COMPLETED','OVERDUE') NOT NULL DEFAULT 'ASSIGNED';
ALTER TABLE `documentation_items` MODIFY `status` ENUM('PENDING','RECEIVED','WAIVED','NOT_APPLICABLE') NOT NULL DEFAULT 'PENDING';
ALTER TABLE `official_documents` MODIFY `status` ENUM('DRAFT','SIGNED','ISSUED','REVOKED','EXPIRED') NOT NULL DEFAULT 'DRAFT';
ALTER TABLE `publishers` MODIFY `verification_status` ENUM('UNVERIFIED','VERIFIED','REJECTED') NOT NULL DEFAULT 'UNVERIFIED';
ALTER TABLE `document_signatories` MODIFY `signing_status` ENUM('PENDING','SIGNED','REJECTED') NOT NULL DEFAULT 'PENDING';
ALTER TABLE `tashih_reviews` MODIFY `result` ENUM('PASSED','REVISION_REQUIRED','REJECTED') NOT NULL;
ALTER TABLE `assignments` MODIFY `stage` ENUM('INITIAL','REVISION','DUMMY') NOT NULL;
SET SESSION sql_mode = @previous_sql_mode;
